description: "A software mechanism that defines the way that files are named, stored, organized, and accessed on logical volumes of partitioned memory"
displayName: "Filesystem"
properties:
  - mountPoint:
    field: filesystem.mountPoint
  - filesystemType:
  # - volume:
  #   multi: true
  - totalStorageSpace:
    field:
        description: Total Storage Space in Bytes
        displayName: Size
        type: int64
  - isEncrypted:
    field:
        description: Whether or not a filesystem is encrypted
        displayName: Encrypted
        type: boolean
