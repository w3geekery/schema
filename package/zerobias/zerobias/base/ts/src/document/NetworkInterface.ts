import { MacAddress } from "@zerobias-org/types-core-js";
import { IpInterface } from "../document/IpInterface";
import { AdminStatus } from "../enum/AdminStatus";
import { NetworkInterfaceType } from "../enum/NetworkInterfaceType";
import { OperationalStatus } from "../enum/OperationalStatus";

// This file is autogenerated - Please do not edit 

export interface NetworkInterface {
  /** Administrative status of the Interface. Available states are: Enabled/Up (1), Disabled/Down (2), and Testing (3) */
  adminStatus?: AdminStatus;
  /** Description for a requirement */
  description?: string;
  /** Official name of device seen by operating system */
  deviceName: string;
  /** IPv4/v6 Interface Properties */
  ipInterface?: IpInterface | Array<IpInterface>;
  /** Boolean value indicating receive permission */
  isReceiveOnly?: boolean;
  /** A hardware MAC address */
  macAddress?: MacAddress;
  /** Maximum Transmission Unit (MTU) measured in bytes */
  mtu?: number;
  /** Specifies types of network interfaces as defined by the Institute of Electrical and Electronics Engineers (IEEE) standards */
  networkInterfaceType?: NetworkInterfaceType;
  /** Current operational state of a resource or interface. */
  operationalStatus?: OperationalStatus;
  /** A numeric value indicating speed measured in bits per second (bps) */
  speed?: number;
  /** A broadcast domain that is partitioned and isolated within a network at the data link layer. A single physical local area network (LAN) can be logically partitioned into multiple, independent VLANs; a group of devices on one or more physical LANs can be configured to communicate within the same VLAN, as if they were attached to the same physical LAN. */
  vlanId?: number;
}
