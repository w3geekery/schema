description: 'Code of the SecurityCredential being claimed (e.g. "ISC2.CISSP"), from the zerobias.schemas.credentials catalog. Held as a plain identifier rather than a link because the catalog now lives in a separate schema package.'
displayName: 'Security Credential'
type: string
