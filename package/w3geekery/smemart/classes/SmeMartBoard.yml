description: "Structural container for tasks. Provides rank, issue numbering, and permission inheritance from parent."
extends:
  - Object
properties:
  - scope:
    field: board.scope
  - partition:
    field: board.partition
  - project:
    linkTo: SmeMartProject.id.boards
  - tasks:
    linkTo: SmeMartTask.id.board
    multi: true
viewProperties:
  "Name":
    jsonata: name
    sort: name
  "Scope":
    jsonata: scope
    sort: scope
  "Partition":
    jsonata: partition
    sort: partition
